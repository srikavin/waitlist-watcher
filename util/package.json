{
  "name": "utils",
  "version": "1.0.0",
  "description": "",
  "main": "populate_historical_events.ts",
  "scripts": {
    "build": "tsc -b",
    "populate_historical_events": "npm run build && node populate_historical_events.js",
    "populate_historical_course_listing": "npm run build && node populate_historical_course_listing.js",
    "move_to_cloud_storage": "npm run build && node move_to_cloud_storage.js",
    "move_files": "npm run build && node move_files.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.27.2",
    "fast-json-patch": "^3.1.1",
    "firebase-admin": "^11.0.1",
    "typescript": "^4.6.3"
  }
}
